| severity | file:area | issue | fix |
|---|---|---|---|
| CRITICAL | [AskEngine.swift](/Users/z/CallBrain/Sources/CallBrainCore/Answer/AskEngine.swift:72) citation validation | Generated prose is returned as `answered` even when factual sentences have no valid `[S#]`; if no tags are used it attaches all sources as a fallback, so claims can be displayed without backing chunks. | Add deterministic post-gen validation: require every factual sentence to cite valid source IDs, strip/quarantine unsupported claims, refuse when validation leaves no supported answer. |
| HIGH | [SearchEngine.swift](/Users/z/CallBrain/Sources/CallBrainCore/Retrieve/SearchEngine.swift:35) / [Vector.swift](/Users/z/CallBrain/Sources/CallBrainCore/Retrieve/Vector.swift:51) refusal floor | Unrelated queries can still return vector hits with cosine `0`, making retrieval non-empty and allowing an LLM call instead of the required raw-evidence refusal. | Preserve raw cosine/BM25 signals, drop below-floor semantic hits, and refuse before LLM when `max_cos < 0.35` and no BM25 hit. |
| HIGH | [SearchEngine.swift](/Users/z/CallBrain/Sources/CallBrainCore/Retrieve/SearchEngine.swift:31) hard filters | `candidateChunkIDs` only scopes the vector lane; FTS runs over the whole corpus, so out-of-scope lexical hits can enter fused results. | Apply the hard predicate/candidate set to both FTS and vector lanes before RRF. |
| HIGH | [Store.swift](/Users/z/CallBrain/Sources/CallBrainCore/Store/Store.swift:188) empty candidate set | `chunkIDs: []` falls through to “all vectors,” so an empty hard-filter result can hydrate unrelated evidence. | Treat `nil` as all vectors and `[]` as no vectors. |
| MEDIUM | [Store.swift](/Users/z/CallBrain/Sources/CallBrainCore/Store/Store.swift:197) vector hydration | Bad embedding blobs are silently skipped with `compactMap`, which can drop in-scope results without surfacing corruption. | Throw/log a structured store error with `chunk_id`, or mark embeddings for rebuild. |
| MEDIUM | [IngestEngine.swift](/Users/z/CallBrain/Sources/CallBrainCore/Ingest/IngestEngine.swift:63) / [Embedder.swift](/Users/z/CallBrain/Sources/CallBrainCore/Embedding/Embedder.swift:51) embedding decode | Embedder only checks the first vector dimension, and ingest silently persists fewer embeddings if the provider returns fewer rows. | Validate vector count equals chunk count and every vector has expected `dim`; fail the ingest or park for review. |
| MEDIUM | [LLMProvider.swift](/Users/z/CallBrain/Sources/CallBrainCore/Providers/LLMProvider.swift:72) pipe handling | stdout/stderr drains start after the full stdin write; a large prompt plus early stderr/stdout can block before drains begin. | Start drain tasks before writing stdin, write stdin asynchronously, then close stdin and await all handles. |
| MEDIUM | [LLMProvider.swift](/Users/z/CallBrain/Sources/CallBrainCore/Providers/LLMProvider.swift:112) Swift 6 concurrency | `ProcHolder: @unchecked Sendable` sends unsynchronized `Process`/`Pipe` references across global queues; only buffers/flag are locked. | Confine subprocess state to a serial executor/actor or locked wrapper; avoid sending raw `Process`/`Pipe` across queues. |

VERDICT: FAIL